// A language file shaped for a lazy loader: the dictionary is the default
// export, so `() => import("./turkish-dictionary.js")` is the whole loader.
export default {
    save: "Kaydet",
    greet: "Merhaba {name}",
};
